#include <stdio.h>

/**
 * _puts - Print a string to the standard output
 *
 * @s:  Pointer to the first character
 */

void _puts(char *str)
{
	while (*s != '\0')
	{
		putchar(*s);
		s++;
	}
	putchar('\n');
}
