#include "main.h"
/**
 * _isupper - prints a string using putchar
 *
 * @c: to be returned
 * Return: 0 (success)
 */
int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))
	{
		return (1);
	}
		return (0);
	}
}

