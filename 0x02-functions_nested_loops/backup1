#include <stdio.h>

/**
 *main -Entry point
 *Return: Always 0 (success)
 */
void print_alphabet(void)
{
	char c = 'a';
	do
	{
		putchar(c);
		c++;
	}
	while(c <= 'z');
}

int main(void)
{
	print_alphabet();

	return (0);
}
